# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password_here
DB_NAME=bookReviewDb

# JWT Secret Key (use a strong secret in actual .env)
JWT_SECRET=your_jwt_secret_here

# Server Port
PORT=5000
